export default class AuthService {
    public login = async (username: string, password: string) => {
        // Database implementation
        // If user not found return always null

        return {
            // MockData
            id: 1,
            username: "mock"
        }

    }
}